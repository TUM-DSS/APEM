# Overview

The `apem` package contains the orchestration and configuration layer of the project:

- `config_loader`: reads and validates runtime configuration.
- `execution_chain`: entry orchestration for solve and analysis workflows.
- `core`: top-level model selection enums.
- `market_models`: model abstractions.
- `unit_based_model` and `order_book_based_model`: model package roots.
